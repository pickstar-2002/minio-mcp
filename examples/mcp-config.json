{
  "mcpServers": {
    "minio-storage": {
      "command": "npx",
      "args": [
        "@pickstar-2002/minio-storage-mcp@latest",
        "--endpoint=localhost",
        "--port=9000",
        "--access-key=minioadmin",
        "--secret-key=minioadmin",
        "--use-ssl=false"
      ]
    }
  }
}